---
title: 'GET v1/stations'
description: 'Retrieve all seismic monitoring stations from the INGV network'
---

## Endpoint

```
GET https://api.terraquakeapi.com/v1/stations
```

## Description

Retrieves all available seismic monitoring stations from the INGV (Istituto Nazionale di Geofisica e Vulcanologia) network. The data is sourced from the FDSN Station service and includes complete metadata for each station including location, elevation, network affiliation, and operational status.

Supports pagination for efficient data retrieval from the complete network of hundreds of stations.

## Use Cases

- **Network Overview**: Get a complete list of all monitoring stations
- **Data Analysis**: Analyze station distribution and network coverage
- **Integration**: Sync station metadata with local databases
- **Research**: Study network evolution and station deployment patterns

## Parameters

<ParamField
  query='limit'
  type='integer'
  default='50'
>
  Number of stations to return per page. Must be a positive integer greater than
  0. **Maximum**: 1000 **Example**: `?limit=100`
</ParamField>

<ParamField
  query='page'
  type='integer'
  default='1'
>
  Page number for pagination. Must be a positive integer greater than 0.
  **Example**: `?page=2`
</ParamField>

## Request Examples

<CodeGroup>

```bash cURL
curl "https://api.terraquakeapi.com/v1/stations?limit=50&page=1"
```

```javascript JavaScript
const response = await fetch(
  'https://api.terraquakeapi.com/v1/stations?limit=50&page=1',
);
const data = await response.json();

if (data.success) {
  console.log(`Total stations: ${data.totalStations}`);
  console.log(
    `Retrieved ${data.payload.length} stations on page ${data.pagination.page}`,
  );

  data.payload.forEach((station) => {
    console.log(`${station.$.code} - ${station.Site.Name}`);
  });
}
```

```python Python
import requests

url = "https://api.terraquakeapi.com/v1/stations"
params = {"limit": 50, "page": 1}

response = requests.get(url, params=params)
data = response.json()

if data.get('success'):
    print(f"Total stations: {data.get('totalStations')}")
    for station in data.get('payload', []):
        code = station.get('$', {}).get('code')
        name = station.get('Site', {}).get('Name')
        print(f"{code} - {name}")
```

```php PHP
<?php
$url = "https://api.terraquakeapi.com/v1/stations?limit=50&page=1";
$response = file_get_contents($url);
$data = json_decode($response, true);

if ($data['success']) {
    echo "Total stations: " . $data['totalStations'] . "\n";

    foreach ($data['payload'] as $station) {
        $code = $station['$']['code'];
        $name = $station['Site']['Name'];
        echo "$code - $name\n";
    }
}
?>
```

</CodeGroup>

## Response Format

<ResponseField
  name='success'
  type='boolean'
>
  Indicates if the request was successful
</ResponseField>

<ResponseField
  name='code'
  type='integer'
>
  HTTP status code (200 for success)
</ResponseField>

<ResponseField
  name='status'
  type='string'
>
  HTTP status message
</ResponseField>

<ResponseField
  name='message'
  type='string'
>
  Human-readable description of the response
</ResponseField>

<ResponseField
  name='payload'
  type='array'
>
  Array of station objects (see Station Object structure below)
</ResponseField>

<ResponseField
  name='totalStations'
  type='integer'
>
  Total number of stations in the network
</ResponseField>

<ResponseField
  name='pagination'
  type='object'
>
  Pagination metadata
  <ResponseField
    name='page'
    type='integer'
  >
    Current page number
  </ResponseField>
  <ResponseField
    name='totalPages'
    type='integer'
  >
    Total number of pages available
  </ResponseField>
  <ResponseField
    name='limit'
    type='integer'
  >
    Number of results per page
  </ResponseField>
  <ResponseField
    name='hasMore'
    type='boolean'
  >
    Whether more pages are available
  </ResponseField>
</ResponseField>

<ResponseField
  name='meta'
  type='object'
>
  Request metadata
  <ResponseField
    name='method'
    type='string'
  >
    HTTP method used
  </ResponseField>
  <ResponseField
    name='path'
    type='string'
  >
    Request path
  </ResponseField>
  <ResponseField
    name='timestamp'
    type='string'
  >
    ISO 8601 timestamp of the request
  </ResponseField>
</ResponseField>

## Station Object

Each station in the `payload` array has the following structure:

<ResponseField
  name='$'
  type='object'
>
  Station metadata attributes
  <ResponseField
    name='code'
    type='string'
  >
    Unique station code (e.g., "ACATE", "IV.ACER")
  </ResponseField>
  <ResponseField
    name='name'
    type='string'
  >
    Station name
  </ResponseField>
  <ResponseField
    name='restrictedStatus'
    type='string'
  >
    Operational status: "open", "closed", "inactive", "deprecated", or
    "unavailable"
  </ResponseField>
  <ResponseField
    name='startDate'
    type='string'
  >
    ISO 8601 date when the station became operational
  </ResponseField>
  <ResponseField
    name='endDate'
    type='string'
    optional
  >
    ISO 8601 date when the station was closed (if applicable)
  </ResponseField>
</ResponseField>

<ResponseField
  name='Latitude'
  type='float'
>
  Station latitude in decimal degrees (-90 to 90)
</ResponseField>

<ResponseField
  name='Longitude'
  type='float'
>
  Station longitude in decimal degrees (-180 to 180)
</ResponseField>

<ResponseField
  name='Elevation'
  type='float'
>
  Station elevation above sea level in meters
</ResponseField>

<ResponseField
  name='Site'
  type='object'
>
  Site information
  <ResponseField
    name='Name'
    type='string'
  >
    Human-readable site location name
  </ResponseField>
</ResponseField>

<ResponseField
  name='CreationDate'
  type='string'
>
  ISO 8601 timestamp when the station record was created
</ResponseField>

## Response Example

```json
{
  "success": true,
  "code": 200,
  "status": "OK",
  "message": "List of seismic monitoring stations (INGV Network)",
  "payload": [
    {
      "$": {
        "code": "ACATE",
        "name": "IV.ACATE",
        "restrictedStatus": "open",
        "startDate": "2015-06-10T00:00:00"
      },
      "Latitude": 37.0287,
      "Longitude": 14.4983,
      "Elevation": 206.0,
      "Site": {
        "Name": "Acate (RG)"
      },
      "CreationDate": "2015-06-10T00:00:00"
    },
    {
      "$": {
        "code": "ACER",
        "name": "IV.ACER",
        "restrictedStatus": "open",
        "startDate": "2009-03-12T00:00:00"
      },
      "Latitude": 40.7328,
      "Longitude": 15.9428,
      "Elevation": 652.0,
      "Site": {
        "Name": "Acerenza (PZ)"
      },
      "CreationDate": "2009-03-12T00:00:00"
    }
  ],
  "meta": {
    "method": "GET",
    "path": "/v1/stations?limit=2&page=1",
    "timestamp": "2025-11-06T10:30:15.240Z"
  },
  "totalStations": 450,
  "pagination": {
    "page": 1,
    "totalPages": 225,
    "limit": 2,
    "hasMore": true
  }
}
```

## Error Responses

### Invalid Limit Parameter

```json
{
  "success": false,
  "code": 400,
  "status": "Bad Request",
  "message": "The 'limit' parameter must be a positive integer greater than 0. Example: ?limit=50"
}
```

### Invalid Page Parameter

```json
{
  "success": false,
  "code": 400,
  "status": "Bad Request",
  "message": "The 'page' parameter must be a positive integer greater than 0. Example: ?page=2"
}
```

### Service Unavailable

```json
{
  "success": false,
  "code": 500,
  "status": "Internal Server Error",
  "message": "HTTP error! status: 503 - INGV service temporarily unavailable"
}
```

## Pagination Example

To retrieve all stations, iterate through pages:

```javascript
async function getAllStations() {
  const allStations = [];
  let page = 1;
  let hasMore = true;

  while (hasMore) {
    const response = await fetch(
      `https://api.terraquakeapi.com/v1/stations?limit=100&page=${page}`,
    );
    const data = await response.json();

    if (data.success) {
      allStations.push(...data.payload);
      hasMore = data.pagination.hasMore;
      page++;
    } else {
      break;
    }
  }

  console.log(`Retrieved ${allStations.length} total stations`);
  return allStations;
}
```

## Related Endpoints

<CardGroup cols={2}>
  <Card
    title='Get Station by Code'
    icon='magnifying-glass'
    href='/api/stations/by-code'
  >
    Search for a specific station
  </Card>

  <Card
    title='GeoJSON Format'
    icon='map'
    href='/api/stations/geojson'
  >
    Get stations for mapping
  </Card>

  <Card
    title='Active Stations'
    icon='circle-check'
    href='/api/stations/status-open'
  >
    Filter for operational stations
  </Card>

  <Card
    title='Statistics'
    icon='chart-simple'
    href='/api/stations/statistics'
  >
    Get network metrics
  </Card>
</CardGroup>
